const initialState = {
    Rooms :[],
    Room:{}
}

export default function rootReducer(state = initialState, action) {
    switch (action.type) {
        case 'GET_ALL_ROOMS':
            
            break;
    
        default:
            break;
    }
}